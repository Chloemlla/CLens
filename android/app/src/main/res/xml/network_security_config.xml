<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- MongoDB endpoints are often LAN cleartext; TLS is still preferred when available. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>